<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RPKPolyclinic" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">@color/main</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Button.DarkRed" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/main</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/gray</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/main</item>
        <item name="colorControlNormal">@color/main</item>
        <item name="colorControlActivated">@color/main</item>
    </style>

    <style name="AppBarLabel" parent="Theme.RPKPolyclinic">
        <item name="android:textColorPrimary">#fff</item>
        <item name="fontFamily">@font/sfui_semibold</item>
    </style>

    <style name="DatePickerDialogStyle" parent="Theme.RPKPolyclinic">
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <style name="SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splashscreen</item>
        <item name="colorPrimaryDark">@color/primary_variant</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
    </style>
</resources>